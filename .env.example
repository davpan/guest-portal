# Site password for authentication
# Required in production. Set this in GitHub repository secrets as VITE_SITE_PASSWORD
VITE_SITE_PASSWORD=your-secure-password-here

# WiFi credentials
# Required in production. Set these in GitHub repository secrets
VITE_WIFI_NETWORK=your-wifi-network-name
VITE_WIFI_PASSWORD=your-wifi-password

# Garage code
# Required in production. Set this in GitHub repository secrets
VITE_GARAGE_CODE=your-garage-code
